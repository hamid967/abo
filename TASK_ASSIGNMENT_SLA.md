# التعيين التلقائي وساعات SLA للمهام

ينشئ أبو مشعل المهام القابلة للتنفيذ من خطوة Playbook المنشورة عند إنشاء الطلب. لكل خطوة قاعدة تعيين وساعات SLA اختيارية، ويحفظ النظام مصدر التعيين وموعد SLA في المهمة نفسها حتى يمكن تدقيقها لاحقاً.

| قاعدة التعيين | السلوك |
| --- | --- |
| `transaction_assignee` | يستخدم موظف المعاملة إذا كان معيناً؛ وإن لم يكن، ينتقل إلى الموظف التشغيلي الأقل حملاً |
| `least_loaded_staff` | يختار أقل مستخدم حملاً بين أدوار `employee` و`supervisor` وفق المهام المفتوحة المعيّنة |
| `request_owner` | يعيّن المهمة لصاحب الطلب عندما يكون الإجراء مطلوباً منه |
| `unassigned` | ينشئ المهمة بلا مسؤول حتى يعيّنها فريق التشغيل يدوياً |

| أولوية الطلب | SLA الافتراضي إذا لم تحدد الخطوة مهلة |
| --- | --- |
| عاجلة | 4 ساعات |
| عالية | 8 ساعات |
| عادية | 24 ساعة |
| منخفضة | 72 ساعة |

تُدخل المهلة في Playbook بالساعات، وتتحول إلى دقائق ووقت استحقاق محفوظ عند إنشاء المهمة. لا يغيّر النظام التعيين اليدوي الموجود، ولا يغير مهام الطلبات السابقة بعد نشر إصدار جديد. يمنع الفهرس الفريد إنشاء مهمة مكررة لنفس خطوة Playbook والمعاملة.

> SLA أداة متابعة داخلية؛ لا تمثل التزاماً من جهة حكومية أو موعداً رسمياً خارج المنصة.
