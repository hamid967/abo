# خطة تصميم «متابع المعاملات الحكومية»

## الغرض والنطاق

يُصمَّم التطبيق كأداة شخصية عربية تعمل محلياً على الجهاز لتجميع المعاملات الحكومية ومتابعة تقدمها. يعطي التصميم الأولوية لقراءة الحالة التالية، معرفة الموعد الأقرب، وإضافة معاملة جديدة بيد واحدة. لا يفترض التطبيق وجود تسجيل دخول أو ربط مباشر بمنصات حكومية؛ فالمستخدم هو من يسجل البيانات المرجعية التي يرغب في متابعتها.

## قائمة الشاشات

| الشاشة | المحتوى الرئيسي | الوظائف الأساسية |
|---|---|---|
| لوحة المتابعة | تحية مختصرة، عداد المعاملات، بطاقة الموعد الأقرب، قائمة المعاملات الحديثة | الانتقال إلى إضافة معاملة، فتح تفاصيل معاملة، تغيير مرشح الحالة |
| قائمة المعاملات | قائمة قابلة للتمرير تضم الجهة، رقم المرجع، الحالة، وتاريخ آخر تحديث | البحث البصري بحسب الحالة، فتح تفاصيل السجل |
| إضافة / تعديل معاملة | نموذج يتضمن اسم المعاملة، الجهة، الرقم المرجعي، الحالة، تاريخ الموعد والملاحظات | حفظ البيانات محلياً، التحقق من الحقول الأساسية، تعديل سجل موجود |
| تفاصيل المعاملة | ملخص الحالة، الرقم المرجعي، الموعد، الملاحظات، ومخطط مراحل المتابعة | تعديل المعاملة، تقديم الحالة أو إعادة ضبطها، حذفها بعد تأكيد المستخدم |
| الإعدادات | خيار مظهر التطبيق ومعلومة عن الخصوصية المحلية | تغيير النمط وقراءة توضيح أن البيانات محفوظة على الجهاز |

## تدفقات الاستخدام الأساسية

| الهدف | المسار |
|---|---|
| تسجيل معاملة جديدة | لوحة المتابعة ← زر «إضافة معاملة» ← تعبئة الحقول ← «حفظ المعاملة» ← العودة إلى لوحة المتابعة مع ظهورها في القائمة |
| معرفة ما يحتاج متابعة | فتح التطبيق ← مراجعة بطاقة «الموعد الأقرب» ← لمس المعاملة ← مراجعة التفاصيل والملاحظات |
| تحديث حالة معاملة | قائمة المعاملات ← اختيار معاملة ← «تحديث الحالة» ← اختيار المرحلة التالية ← حفظ ← تحديث الشارة والعداد |
| تصحيح بيانات سجل | تفاصيل المعاملة ← «تعديل» ← تعديل الحقول ← حفظ ← عرض الملخص المحدَّث |

## هيكل البيانات المحلي

| الحقل | الوصف |
|---|---|
| `id` | معرّف محلي فريد للمعاملة |
| `title` | اسم المعاملة، مثل «تجديد رخصة» |
| `agency` | الجهة الحكومية المختصة |
| `reference` | الرقم المرجعي أو رقم الطلب |
| `status` | جديد، قيد المراجعة، مطلوب إجراء، مكتمل، أو متأخر |
| `dueDate` | تاريخ الموعد أو آخر مهلة اختيارية |
| `updatedAt` | وقت آخر تعديل |
| `notes` | ملاحظات إضافية يضيفها المستخدم |

## اختيارات الواجهة والألوان

يعتمد التطبيق تخطيطاً من اليمين إلى اليسار في وضع عمودي بنسبة 9:16. يبقى الإجراء الرئيسي في متناول الإبهام أسفل الشاشة، وتستخدم البطاقات مساحة بيضاء مريحة مع تباين واضح، على نحو قريب من تطبيقات iOS الأصلية.

| العنصر | اللون | الاستخدام |
|---|---|---|
| الأزرق الحكومي | `#0B5CAD` | الأزرار الأساسية، التبويب النشط، وعناصر الثقة |
| الأزرق الفاتح | `#EAF3FF` | خلفية البطاقات التوضيحية والحالة الجديدة |
| الفحمي | `#172033` | العناوين والنص الأساسي |
| الرمادي الضبابي | `#667085` | النصوص المساندة والبيانات الثانوية |
| الأخضر المتزن | `#15803D` | الحالة المكتملة والمؤشرات الإيجابية |
| الكهرماني | `#B45309` | مطلوب إجراء والمواعيد القريبة |
| الأحمر الهادئ | `#B42318` | الحالة المتأخرة وإجراء الحذف |
| خلفية كريمية باردة | `#F7F9FC` | خلفية الشاشات |

## مبادئ الاستخدام وإتاحة الوصول

تستخدم عناصر اللمس مساحات كافية لليد الواحدة، مع نص عربي كبير نسبيًا وشارات حالة لا تعتمد على اللون وحده. يُعرض الرقم المرجعي بوضوح ويمكن نسخه، بينما تبقى الحقول القابلة للتحرير مركزة في نموذج واحد قصير. ستظهر رسائل تأكيد عند الحفظ والحذف كي لا يواجه المستخدم نقاط نهاية غير واضحة.
