import { useRouter } from "expo-router";
import Ionicons from "@expo/vector-icons/Ionicons";
import { useMemo, useState } from "react";
import { FlatList, Pressable, StyleSheet, View } from "react-native";

import { EmptyState } from "@/components/empty-state";
import { ScreenContainer } from "@/components/screen-container";
import { AppText as Text } from "@/components/ui/app-text";
import { useLocale } from "@/lib/locale-provider";
import { serviceDecisionSource, serviceDecisionSourceLabel } from "@/lib/service-decision";
import { isTodayActionHidden, nextDayAtNine } from "@/lib/today-actions";
import { useTodayActions } from "@/lib/today-actions-provider";
import { useTransactions } from "@/lib/transactions-provider";

type TodayItem = { id: string; transactionId: string; title: string; reason: string; action: string; source: string; priority: "urgent" | "today" | "week" | "follow_up"; dueDate?: string };

function daysUntil(value?: string) {
  if (!value) return undefined;
  const due = new Date(`${value}T12:00:00`);
  if (Number.isNaN(due.getTime())) return undefined;
  return Math.ceil((due.getTime() - Date.now()) / 86_400_000);
}

export default function TodayScreen() {
  const router = useRouter();
  const { locale, isArabic, direction } = useLocale();
  const { transactions, isLoading } = useTransactions();
  const { actions, dismiss, restore, snooze } = useTodayActions();
  const [showHidden, setShowHidden] = useState(false);
  const text = isArabic ? {
    title: "يومي مع أبو مشعل", subtitle: "الأشياء اللي تحتاج انتباهك اليوم، مبنية على معاملاتك الفعلية.", urgent: "عاجل", today: "اليوم", week: "هذا الأسبوع", follow: "متابعة", open: "فتح السجل", empty: "ما فيه إجراء مطلوب حالياً. إذا أضفت موعد أو معاملة، بتظهر لك هنا بوضوح.", add: "بدء طلب جديد", status: "آخر نشاط", snooze: "تأجيل للغد", dismiss: "إخفاء من يومي", restore: "إظهار الإجراء", showHidden: "المؤجلة والمخفية", hideHidden: "إخفاء المؤجلة", hidden: "مؤجل أو مخفي",
  } : {
    title: "My Day", subtitle: "The items that need your attention, based on your real transactions.", urgent: "Urgent", today: "Today", week: "This week", follow: "Follow-up", open: "Open record", empty: "There is no action required right now. Add a transaction or a due date and it will appear here.", add: "Start a request", status: "Latest activity", snooze: "Snooze to tomorrow", dismiss: "Hide from My Day", restore: "Restore action", showHidden: "Deferred and hidden", hideHidden: "Hide deferred", hidden: "Deferred or hidden",
  };

  const allItems = useMemo<TodayItem[]>(() => transactions
    .filter((transaction) => transaction.status !== "completed" && transaction.status !== "cancelled")
    .map((transaction): TodayItem => {
      const remaining = daysUntil(transaction.dueDate);
      const source = serviceDecisionSourceLabel(serviceDecisionSource(transaction), isArabic);
      if (transaction.status === "awaiting_customer_documents") return { id: `${transaction.id}-documents`, transactionId: transaction.id, title: transaction.title, reason: isArabic ? "تحتاج مستنداً قبل ما تكتمل المتابعة." : "A document is required before follow-up can continue.", action: isArabic ? "إضافة المستند المطلوب" : "Add the required document", source, priority: "urgent", dueDate: transaction.dueDate };
      if (remaining !== undefined && remaining < 0) return { id: `${transaction.id}-overdue`, transactionId: transaction.id, title: transaction.title, reason: isArabic ? `تأخر الموعد ${Math.abs(remaining)} يوم.` : `The due date passed ${Math.abs(remaining)} day(s) ago.`, action: isArabic ? "مراجعة الموعد والإجراء التالي" : "Review the due date and next action", source, priority: "urgent", dueDate: transaction.dueDate };
      if (remaining === 0) return { id: `${transaction.id}-today`, transactionId: transaction.id, title: transaction.title, reason: isArabic ? "موعد المتابعة اليوم." : "The follow-up date is today.", action: isArabic ? "فتح تفاصيل الموعد" : "Open due-date details", source, priority: "today", dueDate: transaction.dueDate };
      if (remaining !== undefined && remaining <= 7) return { id: `${transaction.id}-week`, transactionId: transaction.id, title: transaction.title, reason: isArabic ? `الموعد خلال ${remaining} يوم.` : `The due date is in ${remaining} day(s).`, action: isArabic ? "الاستعداد للموعد" : "Prepare for the due date", source, priority: "week", dueDate: transaction.dueDate };
      return { id: `${transaction.id}-follow`, transactionId: transaction.id, title: transaction.title, reason: isArabic ? `آخر تحديث: ${transaction.updatedAt.slice(0, 10)}.` : `Last update: ${transaction.updatedAt.slice(0, 10)}.`, action: isArabic ? "مراجعة حالة المعاملة" : "Review transaction status", source, priority: "follow_up", dueDate: transaction.dueDate };
    })
    .sort((a, b) => ["urgent", "today", "week", "follow_up"].indexOf(a.priority) - ["urgent", "today", "week", "follow_up"].indexOf(b.priority)), [isArabic, transactions]);

  const actionsByItem = useMemo(() => new Map(actions.map((action) => [action.itemId, action])), [actions]);
  const hiddenCount = useMemo(() => allItems.filter((item) => isTodayActionHidden(actionsByItem.get(item.id))).length, [actionsByItem, allItems]);
  const items = useMemo(() => allItems.filter((item) => showHidden || !isTodayActionHidden(actionsByItem.get(item.id))), [actionsByItem, allItems, showHidden]);

  const labels = { urgent: text.urgent, today: text.today, week: text.week, follow_up: text.follow };
  return <ScreenContainer edges={["top", "bottom", "left", "right"]}><FlatList data={items} keyExtractor={(item) => item.id} contentContainerStyle={styles.content} ListHeaderComponent={<View style={styles.header}><Text style={[styles.title, { writingDirection: direction, textAlign: isArabic ? "right" : "left" }]}>{text.title}</Text><Text style={[styles.subtitle, { writingDirection: direction, textAlign: isArabic ? "right" : "left" }]}>{text.subtitle}</Text>{hiddenCount ? <Pressable accessibilityRole="button" accessibilityLabel={`${text.showHidden}: ${hiddenCount}`} onPress={() => setShowHidden((value) => !value)} style={styles.hiddenToggle}><Ionicons name={showHidden ? "eye-off-outline" : "eye-outline"} size={15} color="#0B5D45" /><Text style={styles.hiddenToggleText}>{showHidden ? text.hideHidden : `${text.showHidden} (${hiddenCount})`}</Text></Pressable> : null}</View>} ListEmptyComponent={isLoading ? <View style={styles.loading}><Text style={styles.loadingText}>{isArabic ? "قاعد نجهز يومك…" : "Preparing your day…"}</Text></View> : <EmptyState onAdd={() => router.push("/request/new" as never)} />} renderItem={({ item }) => { const actionState = actionsByItem.get(item.id); const hidden = isTodayActionHidden(actionState); return <View style={[styles.card, item.priority === "urgent" && styles.urgentCard, hidden && styles.hiddenCard]}><View style={[styles.cardTop, { flexDirection: isArabic ? "row-reverse" : "row" }]}><View style={[styles.priority, item.priority === "urgent" && styles.urgentPriority]}><Text style={[styles.priorityText, item.priority === "urgent" && styles.urgentPriorityText]}>{hidden ? text.hidden : labels[item.priority]}</Text></View><Text style={[styles.date, { writingDirection: direction }]}>{item.dueDate ? new Date(`${item.dueDate}T12:00:00`).toLocaleDateString(locale === "ar" ? "ar-SA" : "en-US", { day: "numeric", month: "short" }) : text.status}</Text></View><Text style={[styles.cardTitle, { writingDirection: direction, textAlign: isArabic ? "right" : "left" }]}>{item.title}</Text><View style={[styles.sourceRow, { flexDirection: isArabic ? "row-reverse" : "row" }]}><Ionicons name="git-branch-outline" size={13} color="#6A8474" /><Text style={[styles.source, { writingDirection: direction }]}>{isArabic ? `المصدر: ${item.source}` : `Source: ${item.source}`}</Text></View><Text style={[styles.reason, { writingDirection: direction, textAlign: isArabic ? "right" : "left" }]}>{item.reason}</Text><Pressable accessibilityRole="button" accessibilityLabel={`${text.open}: ${item.title}`} onPress={() => router.push({ pathname: "/transaction/[id]", params: { id: item.transactionId } })} style={({ pressed }) => [styles.openButton, pressed && styles.pressed]}><Text style={styles.openText}>{item.action}</Text><Ionicons name={isArabic ? "chevron-back" : "chevron-forward"} size={16} color="#0B5D45" /></Pressable>{hidden ? <Pressable accessibilityRole="button" accessibilityLabel={`${text.restore}: ${item.title}`} onPress={() => void restore(item.id)} style={styles.restoreButton}><Text style={styles.restoreText}>{text.restore}</Text></Pressable> : <View style={[styles.actionsRow, { flexDirection: isArabic ? "row-reverse" : "row" }]}><Pressable accessibilityRole="button" accessibilityLabel={`${text.snooze}: ${item.title}`} onPress={() => void snooze(item.id, item.transactionId, nextDayAtNine())} style={styles.snoozeButton}><Text style={styles.snoozeText}>{text.snooze}</Text></Pressable><Pressable accessibilityRole="button" accessibilityLabel={`${text.dismiss}: ${item.title}`} onPress={() => void dismiss(item.id, item.transactionId)} style={styles.dismissButton}><Text style={styles.dismissText}>{text.dismiss}</Text></Pressable></View>}</View>; }} ListFooterComponent={<Pressable onPress={() => router.push("/request/new" as never)} style={({ pressed }) => [styles.addButton, pressed && styles.pressed]}><Ionicons name="add" size={20} color="#FFFFFF" /><Text style={styles.addText}>{text.add}</Text></Pressable>} /></ScreenContainer>;
}

const styles = StyleSheet.create({ content: { alignSelf: "center", maxWidth: 780, padding: 20, paddingBottom: 48, width: "100%" }, header: { marginBottom: 18 }, title: { color: "#17382F", fontSize: 25, fontWeight: "800" }, subtitle: { color: "#66756E", fontSize: 13, lineHeight: 21, marginTop: 5 }, hiddenToggle: { alignItems: "center", alignSelf: "flex-start", backgroundColor: "#F0F7F1", borderColor: "#D3E8D8", borderRadius: 999, borderWidth: 1, flexDirection: "row-reverse", gap: 5, marginTop: 12, paddingHorizontal: 10, paddingVertical: 7 }, hiddenToggleText: { color: "#0B5D45", fontSize: 11, fontWeight: "800", writingDirection: "rtl" }, card: { backgroundColor: "#FFFFFF", borderColor: "#E1E9E3", borderRadius: 20, borderWidth: 1, marginBottom: 11, padding: 15 }, urgentCard: { backgroundColor: "#FFF8EF", borderColor: "#F7D9AB" }, hiddenCard: { backgroundColor: "#F7FAF8", opacity: 0.82 }, cardTop: { alignItems: "center", justifyContent: "space-between" }, priority: { backgroundColor: "#E9F5EC", borderRadius: 999, paddingHorizontal: 9, paddingVertical: 4 }, urgentPriority: { backgroundColor: "#FCE6C8" }, priorityText: { color: "#0B5D45", fontSize: 10, fontWeight: "800", writingDirection: "rtl" }, urgentPriorityText: { color: "#A24A05" }, date: { color: "#7C8B84", fontSize: 10 }, cardTitle: { color: "#17382F", fontSize: 15, fontWeight: "800", marginTop: 11 }, sourceRow: { alignItems: "center", gap: 5, marginTop: 8 }, source: { color: "#6A8474", fontSize: 10, fontWeight: "800" }, reason: { color: "#566D61", fontSize: 12, lineHeight: 19, marginTop: 5 }, openButton: { alignItems: "center", alignSelf: "flex-end", flexDirection: "row-reverse", gap: 4, marginTop: 12, paddingVertical: 3 }, openText: { color: "#0B5D45", fontSize: 12, fontWeight: "800", writingDirection: "rtl" }, actionsRow: { alignItems: "center", gap: 8, justifyContent: "flex-start", marginTop: 10 }, snoozeButton: { backgroundColor: "#E9F5EC", borderRadius: 10, paddingHorizontal: 10, paddingVertical: 7 }, snoozeText: { color: "#0B5D45", fontSize: 10, fontWeight: "800", writingDirection: "rtl" }, dismissButton: { borderColor: "#D8E2DA", borderRadius: 10, borderWidth: 1, paddingHorizontal: 10, paddingVertical: 7 }, dismissText: { color: "#62776B", fontSize: 10, fontWeight: "800", writingDirection: "rtl" }, restoreButton: { alignSelf: "flex-end", backgroundColor: "#E9F5EC", borderRadius: 10, marginTop: 10, paddingHorizontal: 10, paddingVertical: 7 }, restoreText: { color: "#0B5D45", fontSize: 10, fontWeight: "800", writingDirection: "rtl" }, addButton: { alignItems: "center", backgroundColor: "#0B5D45", borderRadius: 15, flexDirection: "row-reverse", gap: 7, justifyContent: "center", marginTop: 8, paddingVertical: 14 }, addText: { color: "#FFFFFF", fontSize: 14, fontWeight: "800", writingDirection: "rtl" }, loading: { alignItems: "center", paddingVertical: 48 }, loadingText: { color: "#66756E", fontSize: 13, writingDirection: "rtl" }, pressed: { opacity: 0.76 } });
