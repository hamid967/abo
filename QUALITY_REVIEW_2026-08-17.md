# مراجعة الجودة — أبو مشعل

**التاريخ:** 17 أغسطس 2026  
**النطاق:** بنية التطبيق، الاعتمادات، المصادقة، سجل الجلسات، التوافق مع Expo، الاختبارات، والاستقرار التشغيلي.

## النتيجة التنفيذية

تظهر المراجعة أن المسارات الأساسية للتطبيق محمية باختبارات انحدار واسعة، وأن فحوصات TypeScript وESLint والاختبارات الآلية اجتازت بنجاح بعد الإصلاحات. لم يُظهر تدقيق اعتمادات الإنتاج تنبيهات عالية الشدة. تبقى مخاطر الجودة العملية مرتبطة باختبارات البناء والأجهزة الأصلية، لا بمنطق الواجهة أو الخادم الذي أمكن تشغيله محلياً.

| المحور | الحالة | الدليل القابل للتحقق |
|---|---|---|
| الأنواع والواجهة | ناجح | `pnpm check` و`pnpm lint` ناجحان. |
| الاختبارات | ناجح | 51 ملف اختبار و148 اختباراً ناجحاً، دون اختبارات متجاوزة. |
| الاعتمادات الأصلية | محسّن | أضيف `expo-asset`، وأضيفت إضافتا `expo-asset` و`expo-web-browser` لتهيئة Expo، وتحدثت حزم SDK 54 المتوافقة. |
| الأمان والجلسات | محسّن | أصبحت دالة نطاق الكعكة تتعامل مع مضيف غير معرّف بإنشاء كعكة host-only بدلاً من رمي خطأ. |
| توافق Expo | تنبيه تشغيلي | يمر Expo Doctor في 17 من 18 فحصاً؛ تبقى ملاحظة نطاق إصداري لـReact Navigation رغم توافقه مع تبعياته النظيرة الحالية. |

## الإصلاحات المنفذة

تمت معالجة اعتماد `expo-asset` المطلوب من `expo-audio` في البنى الأصلية، وربط الإضافات المطلوبة في `app.config.js`. كما حدثت حزم Expo SDK 54 إلى نطاقات التصحيح المتوافقة، الأمر الذي أزال التكرار الأصلي لـ`expo-constants` ومشكلة الاعتماد النظير المفقود.

أظهر تفعيل اختبار تسجيل الخروج أن `getSessionCookieOptions` كان يفترض وجود `req.hostname` حتى في سياق موثوق داخل الاختبارات. عُدّل منطق تحديد النطاق ليرجع `undefined` عندما لا يتوفر المضيف، مما يحافظ على كعكة host-only الآمنة، ثم فُعّل الاختبار بنجاح.

> رسائل `Invalid session cookie` للمستخدم غير المسجل ورسائل إنهاء مراقب TypeScript ليست أعطالاً وظيفية للتطبيق. الثانية ناتجة عن ضغط الذاكرة عند تشغيل مراقب منفصل بجانب Expo، ولذلك ينبغي إبقاء فحوصات TypeScript في وضع غير مراقب عند التحقيق أو البناء.

## التوصيات مرتبة حسب الأثر

| الأولوية | التوصية | سببها | معيار الإغلاق |
|---|---|---|---|
| عالية | تنفيذ اختبار قبول حقيقي على iPhone وAndroid لجلسة الدخول، البصمة، التقويم، رفع الملف، وإشعار المهمة. | هذه الوحدات أصلية ولا تحاكيها اختبارات Node بالكامل. | توثيق النتيجة على جهازين مع لقطة لكل تدفق. |
| عالية | إكمال اعتماد FCM وAPNs أو إبقاؤهما صريحين في حالة غير متصل. | لا يمكن التحقق من دفع الإشعارات دون بيانات اعتماد بناء وأجهزة فعلية. | وصول إشعار اختبار مع إيصال تسليم على كل منصة. |
| متوسطة | إضافة مسار CI غير مراقب: `pnpm check && pnpm test && pnpm lint && npx expo-doctor`. | يمنع انحدارات الاعتمادات قبل الدمج أو البناء. | تشغيل ناجح تلقائي لكل تغيير. |
| متوسطة | تقييم تحديث React Navigation ضمن ترقية SDK مخططة بدلاً من فرض خفض النسخة. | التخفيض القسري خلق تعارضات peer؛ النسخة الحالية متسقة مع تبعياتها النظيرة رغم ملاحظة Expo Doctor. | قرار موثق بعد اختبار بناء أصلي. |
| منخفضة | تدوير أو أرشفة سجل التطوير محلياً بعد كل مراجعة كبرى. | `devserver.log` تجاوز 1 ميغابايت خلال جلسات التطوير الطويلة. | سياسة تدوير بسيطة أو تنظيف غير هدّام. |

## خطة اختبار ما قبل الإصدار

ابدأ بفحص غير مراقب في بيئة نظيفة، ثم أنشئ Development Build. على iOS اختبر Face ID والتقويم وعودة OAuth؛ وعلى Android اختبر إذن الإشعار وقناة التنبيه واختيار المستندات. بعد تهيئة FCM وAPNs، اختبر إرسال إشعار مهمة من الخادم والتحقق من سجل التسليم وإعادة فتح شاشة المهمة من التنبيه.
