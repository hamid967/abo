# نموذج أحداث وأتمتة أبو مشعل

تعامل الأتمتة الأحداث باعتبارها حقائق خادمية غير قابلة للتعديل، ثم تقيم قواعد منشورة عليها وتسجل نتيجة مستقلة لكل قاعدة. لا تتصل القواعد مباشرة بأي منصة حكومية أو قناة دفع أو قناة مراسلة خارجية.

| المكوّن | الغرض | مفتاح الحماية |
| --- | --- | --- |
| `automation_events` | حفظ الحدث والسياق الأدنى اللازم للتشغيل. | مفتاح منع تكرار الحدث. |
| `automation_rules` | تعريف القواعد المفعلة وشروطها وإجراءاتها المسموحة. | مفتاح قاعدة فريد، وأولوية صريحة. |
| `automation_runs` | تسجيل نتيجة كل قاعدة على كل حدث. | مفتاح مركب يمنع تشغيل القاعدة مرتين للحدث نفسه. |

تبدأ القواعد الأولى بأحداث داخل التطبيق مثل إنشاء الطلب، طلب التحويل البشري، ربط المستند، وتغير حالة المعاملة. يحظر المحرك تنفيذ قواعد منشأة من نص المستخدم أو إتاحة SQL أو أدوات شبكية مباشرة للنموذج.
