# تسجيل الدخول في Expo Go

## سبب الإصلاح

لا يقبل خادم المصادقة رابط عودة بصيغة `exp://`، كما لا يوفر Expo Go مخطط تطبيق مخصصاً مستقراً لمسارات OAuth. لذلك لم يعد أبو مشعل يطلب من مزود المصادقة العودة إلى تطبيق Expo Go مباشرة.

## التدفق الجديد

عند الضغط على «تسجيل الدخول»، ينشئ التطبيق محاولة مؤقتة مدتها عشر دقائق مع إثبات عشوائي محفوظ بهاتف المستخدم. يفتح المتصفح صفحة Manus OAuth مع رابط عودة HTTPS موثوق. بعد اختيار الحساب، يخزن الخادم **رمز التفويض المؤقت** فقط، ويعرض رسالة تطلب الرجوع إلى Expo Go. يستعلم التطبيق عن النتيجة بالإثبات الخاص به، ثم يستبدل الرمز على الخادم ويحفظ رمز الجلسة في SecureStore.

> لا يوضع رمز الجلسة في رابط العودة، ولا يسمح رابط الإكمال بقراءة النتيجة من دون إثبات المحاولة المطابق.

## التحقق على الهاتف

بعد إعادة تحميل Expo Go، افتح «الحساب والمزامنة» واضغط «تسجيل الدخول». اختر الحساب في المتصفح، ثم ارجع إلى Expo Go عند ظهور رسالة تأكيد المتصفح. ينبغي أن تتبدل الشاشة إلى بيانات الحساب خلال ثوانٍ. إذا انتهت المحاولة أو أُغلق المتصفح، أعد الضغط على «تسجيل الدخول» لإنشاء محاولة جديدة.

| التحقق | النتيجة المتوقعة |
| --- | --- |
| بدء الدخول | لا يظهر خطأ `redirect_uri scheme 'exp' not allowed`. |
| اختيار الحساب | تظهر صفحة تأكيد HTTPS تطلب الرجوع إلى Expo Go. |
| العودة للتطبيق | تحفظ الجلسة محلياً وتظهر بيانات الحساب ودوره. |
| إعادة فتح التطبيق | تبقى الجلسة في SecureStore ويستمر الوصول إلى البيانات المعزولة للحساب. |
