# كتالوج أدوات المساعد التنفيذي

لا يملك النموذج اتصالاً مباشراً بقاعدة البيانات أو SQL أو نقاط نهاية غير مسجلة. يختار الخادم فقط أدوات محددة، ويصادق المستخدم ويتحقق من الملكية ويطبق مخطط الإدخال قبل التنفيذ.

| الأداة | الأثر | الحماية |
| --- | --- | --- |
| `create_request_draft` | إنشاء مسودة | مفتاح منع تكرار مرتبط بالمستخدم. |
| `update_request_draft` | تحديث بيانات منظمة | ملكية المسودة، حقول مسموحة فقط، وتدقيق. |
| `validate_request_draft` | تحقق بلا إرسال | لا يغير الطلب النهائي. |
| `submit_request` | إنشاء طلب نهائي | موافقة صريحة وإصدار ملخص ومفتاح idempotency. |
| `list_user_requests` و`get_request_status` | قراءة المتابعة | حساب المستخدم فقط، أو دور تشغيلي مصرح. |
| `attach_document_to_draft` و`remove_document_from_draft` | ربط المرفقات | ملكية المستند والمسودة وحدود النوع والحجم. |
| `create_inquiry` و`request_human_handoff` | دعم وتحويل بشري | تذكرة مملوكة وسجل تدقيق وإشعار. |

لا توجد أدوات للدفع أو إرسال SMS أو WhatsApp أو الوصول لمنصات حكومية. عند نية `pay_invoice` أو الشكوى الحساسة يقتصر المسار على التحويل البشري.
