# المصادقة البيومترية في أبو مشعل

أضيفت المصادقة بالبصمة أو Face ID كخيار اختياري لفتح الجلسة المحلية بسرعة. يبقى رمز الجلسة داخل `expo-secure-store`، بينما يُحفظ تفضيل التفعيل منفصلاً ولا تُحفظ أي بصمة أو صورة وجه داخل التطبيق.

## التدفق

بعد تسجيل الدخول التقليدي بنجاح، يفتح المستخدم شاشة «الحساب والمزامنة» ويفعّل خيار «دخول أسرع وأكثر أماناً». يطلب التطبيق تحققاً بيومترياً فورياً قبل حفظ التفضيل. عند العودة إلى التطبيق لاحقاً، تُحجب محتويات الجلسة وتظهر شاشة فتح مخصصة؛ وبعد نجاح Face ID أو البصمة تُتاح الجلسة. يؤدي إلغاء التحقق إلى إبقاء الحساب مقفلاً، مع خيار «استخدام تسجيل الدخول التقليدي» الذي ينهي الجلسة المحلية ويعيد مسار الدخول الأساسي.

| الحالة | سلوك التطبيق |
| --- | --- |
| لا يوجد مستشعر أو لا توجد بصمة/وجه مسجل | يبقى تسجيل الدخول التقليدي متاحاً ويظهر سبب عدم الإتاحة. |
| نجاح التحقق | تُفتح الجلسة المخزنة في SecureStore دون إرسال بيانات بيومترية للخادم. |
| إلغاء المستخدم | لا يظهر خطأ مزعج، وتبقى شاشة القفل مع زر إعادة المحاولة. |
| فشل أو قفل المستشعر | تظهر رسالة إرشادية مع بديل تسجيل الدخول التقليدي. |

## الاختبار

يمكن اختبار منطق التطبيق وفحوصات TypeScript والاختبارات الآلية داخل البيئة، لكن Face ID في Expo Go غير مدعوم وفق وثائق Expo؛ يلزم Development Build أو بناء أصلي لاختبار نافذة Face ID الفعلية. يجب تجربة جهاز iOS بوجه مسجل وجهاز Android ببصمة مسجلة، ثم التحقق من التفعيل، القفل عند العودة، النجاح، الإلغاء، والرجوع إلى تسجيل الدخول التقليدي.
