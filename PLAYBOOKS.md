# Playbooks وإدارة إصدارات الخدمات

يضيف مركز Playbooks مسار عمل قابل لإعادة الاستخدام لكل خدمة متاحة. ينشئ الأدمن Playbook واحداً فعالاً للخدمة، ثم يبني داخله إصدارات مسودة تتضمن خطوات مرتبة من أنواع التعليمات أو المستندات أو الموافقات أو المهام. لا يمكن نشر إصدار بلا خطوة واحدة على الأقل.

| الإجراء | الضبط المطبق |
| --- | --- |
| إنشاء Playbook | يقتصر على `admin` و`super_admin`، ويرتبط بخدمة فعالة، ويمنع Playbook فعالاً ثانياً للخدمة نفسها |
| إنشاء إصدار | يولد رقم إصدار تصاعدياً ولا يعدل الإصدارات السابقة |
| النشر | يؤرشف الإصدار المنشور السابق لنفس Playbook وينشر الإصدار المحدد مع سجل تدقيق |
| أرشفة Playbook | توقف Playbook وإصداراته المستقبلية، ولا تغير الطلبات التي التقطت نسخة سابقة |
| إنشاء طلب جديد | يلتقط الإصدار المنشور والخطوات والمتطلبات داخل لقطة JSON مرتبطة بالطلب |

> تعديل Playbook أو نشر إصدار جديد لا يغير الخطوات التي بدأت بها الطلبات السابقة. هذا يحافظ على قابلية التتبع والتدقيق في رحلة العميل.

تتوفر الإدارة من **لوحة متابعة المعاملات** ثم **Playbooks الخدمات**. لا يستطيع العميل تحرير Playbook أو نشره، ولا تتصل الميزة بأي جهة حكومية أو قناة خارجية.

## توليد المهام من الخطوات

عند إنشاء طلب جديد مرتبط بخدمة لها إصدار Playbook منشور، تلتقط المنصة النسخة وخطواتها أولاً ثم تنشئ مهاماً للمستخدم لكل خطوة من أنواع `task` أو `document` أو `approval`. تبقى خطوات `instruction` ضمن لقطة Playbook للإرشاد ولا تتحول إلى مهام قابلة للتنفيذ.

| خاصية المهمة المولدة | السلوك |
| --- | --- |
| المصدر | `playbook_step` مع مفتاح يجمع إصدار Playbook ومفتاح الخطوة |
| المالك | المستخدم صاحب الطلب، ولا تُعيّن لموظف إلا عبر مسار تعيين صريح لاحق |
| الأولوية | ترث أولوية الطلب الجديد |
| الموعد | يعتمد مدة الخطوة المتوقعة عند وجودها؛ وإلا يبقى بلا موعد افتراضي مضلل |
| التكرار | يمنعه فهرس فريد على المعاملة ومصدر المهمة ومفتاح الخطوة، حتى عند إعادة المحاولة |

> توليد المهمة لا يرسل شيئاً إلى جهة خارجية ولا يغير حالة المعاملة تلقائياً. هو إجراء داخلي واضح ومربوط بالإصدار الذي بدأ به الطلب.
