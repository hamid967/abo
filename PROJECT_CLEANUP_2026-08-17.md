# تقرير تنظيف المشروع

**التاريخ:** 17 أغسطس 2026  
**النطاق:** إزالة ملفات القالب غير المستخدمة وإصلاح المراجع الناتجة عن تهيئة Expo، من دون حذف بيانات أو ترحيلات أو سجلات تشغيل لازمة.

## الملفات المحذوفة

حُذفت ملفات قالب Expo التالية بعد البحث في جميع ملفات TypeScript وTSX وملفات الإعداد والتأكد من عدم وجود أي استيراد أو مرجع إليها:

| الفئة | الملفات |
|---|---|
| مكونات قالب غير مستخدمة | `components/external-link.tsx`، `components/hello-wave.tsx`، `components/parallax-scroll-view.tsx`، `components/ui/collapsible.tsx` |
| أصول React التجريبية | `assets/images/partial-react-logo.png`، `assets/images/react-logo.png`، `assets/images/react-logo@2x.png`، `assets/images/react-logo@3x.png` |

لم تُحذف مكونات مثل `themed-view.tsx` لأنها ما زالت مستخدمة في مسار OAuth وبيئة فحص الثيم. كما لم تُحذف ملفات الترقيات وقاعدة البيانات أو سجل خدمة التطوير أو الوثائق التشغيلية، لأنها تمثل سجلاً أو متطلباً تشغيلياً وليس مجرد قالب غير مستخدم.

## إصلاحات مرتبطة بالبناء

حُولت تهيئة Expo من `app.config.ts` إلى `app.config.js` بصيغة CommonJS، كما تحول محمل البيئة إلى `scripts/load-env.cjs`. **تمت مراجعة هذا السجل لاحقاً:** لا يوجد `eas.json` أو معرف مشروع EAS أو اعتماد على Expo Build ضمن المشروع. يبنى Android عبر `expo prebuild` وGradle داخل GitHub Actions، ثم يوقّع من أسرار GitHub فقط.

## التحقق

| الفحص | النتيجة |
|---|---|
| TypeScript | ناجح بلا أخطاء |
| اختبارات Vitest | 44 ملفاً ناجحاً، 125 اختباراً ناجحاً، واختبار واحد متجاوز |
| Expo lint | ناجح |
| فحص المراجع للملفات المحذوفة | لا توجد مراجع مستخدمة |

## عناصر أبقيت عمداً

ملفات `.manus-logs` و`.expo` و`node_modules` محكومة بقواعد التجاهل أو البيئة التشغيلية ولا تُدرج في تسليم المستودع. لم تُحذف الوثائق الفنية أو تقارير المراحل، لأنها توفر سجلاً للقرارات والاختبارات والالتزامات. كما لم تُحذف أي ملفات اعتماد أو مفاتيح؛ قواعد `.gitignore` تحظر هذه الامتدادات أصلاً.
