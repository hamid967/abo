# تغييرات قاعدة البيانات للمساعد التنفيذي

## مبادئ التصميم

تعتمد الجداول الجديدة على معرف المستخدم الحالي لعزل الملكية، وترتبط بالطلب والمعاملة والمستندات القائمة دون نسخها. تستخدم المفاتيح الأجنبية والفهارس، وتستعمل جداول تشغيل منفصلة للأتمتة في مراحل لاحقة. لا يدعم MySQL في هذا المشروع RLS بنفس أسلوب PostgreSQL؛ لذا سيطبّق العزل في جميع استعلامات الخادم عبر `customerUserId` والتحقق المركزي من الصلاحيات.

| الجدول | الغرض | الحقول الحرجة | علاقات العزل |
|---|---|---|---|
| `request_drafts` | المسودة المنظمة قبل الإرسال | `ownerUserId`، `structuredData`، `status`، `summaryVersion`، `idempotencyKey` | المستخدم والطلب النهائي عند الإرسال. |
| `ai_conversations` | حالة رحلة المحادثة وهدفها | `ownerUserId`، `draftId`، `currentState`، `detectedIntent` | مستخدم واحد ومسودة اختيارية. |
| `ai_messages` | رسائل العميل والمساعد دون الأسرار | `conversationId`، `role`، `content`، `metadata` | محادثة مملوكة للحساب. |
| `request_draft_documents` | ربط مرفق موجود بالمسودة | `draftId`، `documentId`، `classificationStatus` | مسودة ومرفق يملكهما المستخدم نفسه. |
| `user_consents` | تسجيل موافقة نسخة ملخص أو سياسة | `ownerUserId`، `draftId`، `consentType`، `summaryVersion` | مستخدم ومسودة محددان. |
| `handoff_requests` | تحويل من المحادثة إلى فريق الدعم | `conversationId`، `draftId`، `ticketId`، `reason` | مستخدم ومحاولة دعم قابلة للتدقيق. |

## مفاتيح منع التكرار

يمتلك `request_drafts.idempotencyKey` فهرساً فريداً لكل مستخدم، ويُربط الطلب النهائي بالمسودة. يمنع ذلك إنشاء طلبين نتيجة ضغط مزدوج أو إعادة محاولة الشبكة. يستخدم إصدار الملخص في الموافقات، لذلك لا تبقى الموافقة صالحة إذا عدل العميل البيانات بعد المراجعة.
