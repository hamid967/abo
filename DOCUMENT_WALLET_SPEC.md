# مواصفة محفظة المستندات

تجمع المحفظة المستندات التي رفعها صاحب الحساب في مكان واحد، مع اسم الملف ونوعه وحجمه وحالة التحقق وتاريخ الرفع وربطه بالطلب أو المعاملة عند وجوده. يعتمد العرض على الاستعلام الخادمي المقيّد بـ `ownerUserId` ولا يكشف مفتاح التخزين أو رابطاً دائماً للملف.

| الإجراء | الضبط المطبق |
| --- | --- |
| الرفع | PDF وJPG وPNG وDOCX فقط، حتى 5 م.ب، مع فحص محتوى Base64 على الخادم وتدقيق حدث الرفع |
| العرض | يصدر الخادم رابط تنزيل موقّعاً بعد تحقق ملكية المستند ويكتب تدقيقاً لطلب الرابط |
| الحذف | حذف منطقي للملف غير المرتبط بسجل؛ يرفض الخادم حذف المستند المرتبط بطلب أو معاملة |
| المعالجة الذكية | لا تحلل المحفظة الملف تلقائياً، ولا ترسل محتواه إلى خدمة ذكاء دون موافقة منفصلة |
| الخصوصية | لا يعرض التطبيق مفتاح التخزين أو بصمة الشبكة أو تفاصيل تقنية حساسة |

> المحفظة تحفظ المستندات ضمن حساب المستخدم في أبو مشعل، ولا تمثل أرشيفاً حكومياً أو بديلاً عن الحفظ النظامي لدى الجهة المختصة.
