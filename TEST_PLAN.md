# خطة الاختبار

| المسار | نوع الاختبار | معيار النجاح |
|---|---|---|
| تسجيل الدخول والدور | يدوي + وحدات | لا تعرض المسارات المحمية أو بيانات الحساب دون جلسة صحيحة |
| عزل العميل | وحدات + تكامل | لا يقرأ العميل طلباً أو تذكرة أو إشعاراً ليس له |
| التذاكر | تكامل | إنشاء تذكرة، إضافة رسالة، وإخفاء الملاحظة الداخلية عن العميل |
| المعرفة | وحدات + واجهة | النشر للمشرف فما فوق، والقراءة للمنشور فقط |
| سجل التدقيق | تكامل | تسجيل العمليات الإدارية، وعدم قراءته إلا للمدير أو المدير العام |
| البحث | تكامل | تقييد نتائج العميل بملكيته وتوسيع نتائج الموظف ضمن تفويضه |
| اللغات | واجهة | تعكس الشاشات الجديدة اتجاهها وتستبدل تسميات النظام عند اختيار الإنجليزية |
| المواعيد والمرفقات | الانحدار | عدم تعطل التذكيرات أو حدود ونوع الملفات الحالية |

لا يستخدم الاختبار بيانات شخصية حقيقية. يجب تشغيل `pnpm check` و`pnpm test` و`pnpm lint` قبل حفظ أي نسخة إصدار.

