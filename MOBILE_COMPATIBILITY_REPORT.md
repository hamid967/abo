# تقرير توافق Android وiPhone وiPad

## نتيجة التدقيق

راجعت النسخة الحالية من أبو مشعل على مستوى إعدادات Expo، المسارات، مناطق الأمان، الميزات الأصلية، وواجهات الإدخال. تعمل الإعدادات على استهداف Android وiOS، مع دعم iPad مفعلاً، ويظل التطبيق بوضع عمودي وفق قرار تصميم تجربة الاستخدام الحالية.

| مجال التحقق | الإصلاح أو التحقق المنفذ | الحالة |
| --- | --- | --- |
| إعدادات المنصات | التحقق من `supportsTablet`، المعرّفات، مخطط الرابط، الإشعارات، والبصمة | مكتمل |
| اختيار المستندات | إضافة ملحق `expo-document-picker` للإعدادات، مع النسخ المؤقت قبل القراءة | مكتمل |
| الشاشات الطويلة | تحويل الإعدادات إلى `ScrollView` وإبقاء اللمسات صالحة أثناء ظهور لوحة المفاتيح | مكتمل |
| iPad | تحديد حد عرض منطقي للإعدادات والمحادثات لتفادي بطاقات متمددة بشكل غير مناسب | مكتمل |
| المحادثات | إضافة `KeyboardAvoidingView` و`keyboardShouldPersistTaps` في محادثة العميل وصندوق الأدمن | مكتمل |
| OAuth | دعم رابط العودة عند كون التطبيق مفتوحاً، ومنع تبادل الرمز مرتين، وتقليل سجلات الرموز والجلسة | مكتمل |
| الجلسة غير المسجلة | التعامل مع 401 و403 كحالة عدم دخول متوقعة بدلاً من طباعة خطأ مزعج | مكتمل |

## حدود الاختبار الفعلي

التحقق البرمجي لا يحل محل الجهاز الفعلي. تتطلب Face ID على iPhone وiPad بناء تطوير أو بناء أصلي، ولا يعتمد عليها في Expo Go [1]. كما أن تجربة اختيار الملفات يجب أن تختبر من موفر ملفات iCloud أو Files على iOS، لأن الرابط المؤقت للملف لا يستمر بعد إغلاق التطبيق [2]. روابط المخطط المخصص تحتاج أيضاً بناء تطوير أو إنتاج للتحقق الفعلي؛ Expo Go لا يوفر رابطاً مستقراً لهذا الغرض [3].

| الجهاز | خطوات القبول المقترحة |
| --- | --- |
| Android 13 أو أحدث | اختبر الإشعارات، البصمة، اختيار PDF وصورة، دخول OAuth، الكتابة في محادثة طويلة، والعودة من الخلفية. |
| iPhone | اختبر العودة من المتصفح، Touch ID أو Face ID عبر Development Build، اختيار ملف من Files، شريط التنقل، ولوحة المفاتيح في المحادثة. |
| iPad | اختبر عرض الإعدادات والمحادثات، تعدد الأسطر في الكتابة، اختيار ملف من Files، ومناطق الأمان في الاتجاه العمودي. |

## نتيجة الجودة

نجحت فحوصات TypeScript، واختبارات المشروع بما فيها اختبار توافق المنصات المضاف، وExpo lint. تحذير ESM الصادر من أداة lint معروف وغير وظيفي؛ لا يُعالج بإضافة `type: module` لأن المشروع يستخدم ملفات إعداد CommonJS أخرى.

## المراجع

[1] [Expo LocalAuthentication](https://docs.expo.dev/versions/latest/sdk/local-authentication/)

[2] [Expo DocumentPicker](https://docs.expo.dev/versions/latest/sdk/document-picker/)

[3] [Expo Linking](https://docs.expo.dev/versions/latest/sdk/linking/)
