# اختبار إشعارات الجوال والتقويم

يصف هذا الدليل تشغيل واختبار ربط **أبو مشعل** لإشعارات مهام SLA، والتنبيهات المحلية، وإشعارات الدفع، وإضافة المهمة إلى تقويم الجهاز. لا يمثل هذا الربط أي جهة حكومية ولا يتصل بمنصات حكومية خارجية.

## ما تم ربطه

| المسار | السلوك |
|---|---|
| التذكير المحلي | يحدد أقرب 30 مهمة غير مكتملة لها موعد SLA أو موعد عام، ويعيد جدولة التنبيه قبل الموعد بالدقائق التي يختارها المستخدم. |
| الدفع متعدد الأجهزة | يسجل كل جهاز iOS أو Android برمز Expo Push مرتبط بالحساب، ثم يرسل الفحص اليومي تنبيه المهمة عند حلول اليوم أو تجاوز الموعد. |
| التنقل من التنبيه | يؤدي الضغط على تنبيه المهمة إلى شاشة متابعة SLA فقط؛ لا يفتح التطبيق رابطاً خارج قائمة المسارات المسموح بها. |
| التقويم | يطلب إذناً صريحاً عند الضغط على «إضافة للتقويم»، ثم ينشئ أو يحدّث حدثاً في تقويم قابل للكتابة مع منبه قبل موعد SLA. |

> **مهم:** نجاح تذكرة Expo يعني أن خوادم Expo استقبلت الطلب، وليس بالضرورة أن المستخدم شاهد التنبيه. يجب فحص إيصالات الدفع، وإيقاف إرسال الرسائل إلى رمز يعيد `DeviceNotRegistered`. [1]

## متطلبات البناء الفعلي

إشعارات الدفع على Android لا تُختبر في Expo Go. استخدم نسخة **Development Build** أو **Production Build** مرتبطة بمشروع Expo/EAS؛ كما يلزم ضبط بيانات APNs لتطبيق iOS وبيانات FCM V1 لتطبيق Android. يجب أن يحتوي تكوين التطبيق في أثناء البناء على معرف مشروع EAS صالح حتى يتمكن العميل من طلب `ExpoPushToken`.[1]

| المنصة | نوع الاختبار المطلوب | تحقق يدوي |
|---|---|---|
| Android | Development أو Production Build | منح إذن الإشعار، تشغيل زر تفعيل إشعارات المهام، تلقي دفع والتأكد من فتح شاشة SLA. |
| iPhone | Development أو Production Build | منح إذن الإشعار والتقويم، تلقي الدفع، إنشاء حدث تقويم، والتأكد من المنبه قبل الموعد. |
| iPad | Development أو Production Build | التحقق من عرض الإعدادات في الوضع العمودي ومن فتح شاشة SLA بعد الضغط على التنبيه. |
| Expo Go | فحص واجهة وتذكيرات محلية فقط عند الدعم | لا يعتمد عليه للتحقق من دفع Android أو بيانات APNs/FCM. |

## سيناريو قبول مختصر

أنشئ أو عيّن مهمة لها `slaDueAt` مستقبلي. من الإعدادات، فعّل إذن الإشعارات ثم فعّل «إشعارات الدفع للمهام». تحقق أن الجهاز المسجل يظهر خادمياً **من دون عرض رمز الدفع للواجهة**. أضف المهمة إلى التقويم، ثم غيّر حالتها إلى مكتملة وتأكد أن التذكير المحلي لا يعاد جدولته. أخيراً شغّل الفحص اليومي أو انتظر دورته، وتحقق من سجل تسليم الإشعار ومن فتح شاشة المتابعة عند الضغط على التنبيه.

## ضوابط التشغيل والأمن

يُفرض عزل الجهاز في الخادم: لا يستطيع العميل إرسال `userId` لتسجيل جهازه، ولا يعيد الخادم رمز الدفع إلى أي واجهة. يزيل التسجيل النشط لرمز مشترك من حساب سابق عند تسجيله في حساب جديد. عند ظهور `DeviceNotRegistered` يتعطل الجهاز المسجل، ويُسجل سبب الفشل. كما أن تفضيلات الدفع، وتنبيهات المهام، وساعات الهدوء تطبق قبل محاولة الإرسال.

لا يُرسل الخادم أكثر من 100 رسالة في طلب Expo واحد، ويجب تطبيق إعادة محاولة متدرجة عند أخطاء الشبكة أو 429 أو 5xx إذا توسع حجم الإرسال. توصي Expo بفحص الإيصالات بعد فترة تقارب 15 دقيقة، وتذكر أنها تُحذف بعد 24 ساعة؛ لذلك يلزم إضافة عامل إيصالات دوري قبل أي تشغيل إنتاجي واسع النطاق.[1]

## ما يزال مطلوباً قبل الإطلاق

يلزم إدخال معرف مشروع EAS الصحيح في تكوين التطبيق وقت البناء، وربط بيانات APNs وFCM V1 مع المشروع، ثم تنفيذ اختبار قبول حقيقي بحسابين منفصلين: حساب يملك المهمة وحساب مكلّف بها. لا تختبر هذه الخطوات أو تثبت التسليم الفعلي من داخل بيئة الويب.

## المراجع

[1]: https://docs.expo.dev/push-notifications/sending-notifications/ "Expo: Send notifications with the Expo Push Service"
