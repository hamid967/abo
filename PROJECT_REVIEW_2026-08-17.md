# مراجعة شاملة لمشروع أبو مشعل

**التاريخ:** 17 أغسطس 2026  
**النطاق:** مراجعة جودة التطبيق والخادم وعزل البيانات والتوافق والاعتمادات، مع إصلاح العناصر التي أمكن التحقق منها داخل بيئة المشروع.

## النتيجة التنفيذية

المشروع اجتاز التحقق البرمجي الكامل بعد الإصلاحات. لا تظهر أخطاء TypeScript أو أخطاء Expo lint، ونجحت **124 حالة اختبار** ضمن 43 ملف اختبار، مع حالة واحدة متجاوزة تتطلب بيئة مختلفة. يظل التطبيق بحاجة إلى اختبار قبول على أجهزة فعلية قبل إعلان جاهزية الإطلاق، خصوصاً لمسارات دفع الإشعارات والتقويم وFace ID.

## الإصلاحات المنفذة

| المجال | الملاحظة | المعالجة |
|---|---|---|
| الاعتمادات الأمنية | أظهر تدقيق الاعتمادات تنبيهات مباشرة لـ tRPC وAxios وDrizzle ORM. | رُقّيت `@trpc/client` و`@trpc/react-query` و`@trpc/server` إلى `11.8.0`، وAxios إلى `1.15.2`، وDrizzle ORM إلى `0.45.2`، ثم اختُبرت مسارات الصلاحيات والعزل. |
| خصوصية السجلات | كانت بعض سجلات المصادقة وقاعدة البيانات تطبع كائن الخطأ كاملاً. | أزيلت طباعة تفاصيل جلسة المصادقة، وأصبحت رسائل قاعدة البيانات عامة ولا تطبع بيانات اتصال أو استعلامات محتملة الحساسية. |
| توافق الواجهة | ظهر تحذير React Native متكرر لاستخدام خاصية `pointerEvents` المهجورة. | نُقلت الخاصية إلى كائن `style` لمكوّن انتقال الثيم. |
| جودة أدوات التطوير | ظهر تحذير ESLint لتفسير ملف إعداد بصيغة ESM داخل مشروع CommonJS. | أُعيدت تسمية الإعداد إلى `eslint.config.mjs`، وانتهى فحص lint بلا تحذير. |
| صياغة أذونات الجوال | كان نص إذن الميكروفون بالإنجليزية ضمن تطبيق عربي. | استُبدل بنص عربي يوضح الاستخدام، مع الإبقاء على أذونات الإشعارات والتقويم والبصمة كما هي. |

## ضوابط تم التحقق منها

تمت مراجعة المسارات الحساسة للمعاملات والمهام والإشعارات. تحديث حالة المهمة مقيد في الخادم بمالك المهمة أو المعيّن لها. مركز الإشعارات يعيد فقط سجلات المستلم الحالي، ولا يرفق ملخص المهمة إلا عندما تكون المهمة مملوكة للمستخدم أو مسندة إليه. كما لا تعيد واجهة أجهزة الدفع رمز Expo إلى العميل.

تمت مراجعة نقاط الوصول العامة، وهي مقتصرة على معرفة حالة الجلسة وتسجيل الخروج. أما العمليات التي تتعامل مع بيانات المستخدم أو أجهزة الدفع أو حالة المهمة فتستعمل إجراءات محمية وتستند إلى المستخدم في جلسة الخادم.

## التحقق المنفذ

| الفحص | النتيجة |
|---|---|
| TypeScript | ناجح بلا أخطاء |
| اختبارات Vitest | 43 ملفاً ناجحاً، 124 اختباراً ناجحاً، اختبار واحد متجاوز |
| Expo lint | ناجح بلا أخطاء أو تحذيرات |
| اختبار عزل الحسابات والصلاحيات | ناجح |
| اختبار توافق Android وiPhone وiPad | ناجح على مستوى الكود والإعدادات |
| اختبار خصوصية سجلات قاعدة البيانات | ناجح |

## ملاحظات تشغيلية وفجوات خارجية

يتطلب التحقق الحقيقي من دفع الإشعارات إعداد APNs وFCM وبناء Development أو Production، ولا يمكن إثباته من Expo Go فقط. كما يحتاج تقويم الجهاز وFace ID وأذونات الإشعار إلى اختبار مباشر على iPhone وAndroid وiPad.

أظهر تدقيق الاعتمادات عدداً كبيراً من التنبيهات المتعدية التابعة لأدوات Expo وCLI. عولجت الاعتمادات المباشرة التي تبيّن لها إصدار مصحح محدد، لكن لا يُنصح بترقية شجرة Expo المتعدية عشوائياً خارج تحديث SDK متوافق ومختبر. كما قد يستهلك مراقب TypeScript الخارجي ذاكرة مؤقتاً في بيئة التطوير؛ شغّل `pnpm check` كفحص منفصل وأوقف أي `pnpm check --watch` إضافي عند ظهور ضغط ذاكرة.

## توصية القبول

النسخة مناسبة لمراجعة فريق الجودة بعد التحقق البرمجي. قبل النشر النهائي، نفّذ اختبار قبول بحسابين على iOS وAndroid، وسجّل نجاح عودة OAuth، وتسجيل جهاز دفع، واستلام تنبيه مهمة، وإضافة حدث تقويم، ثم راجع إيصالات Expo بعد إعداد APNs وFCM.
